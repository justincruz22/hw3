<h1>All Posts!</h1>

  <% for post in @posts %>
    <h2><%= "#{post.title}" %></h2>
    <h3><%= "#{post.posted_on}" %></h3>
    <p><%= "#{post.description}" %></p>
  <% end %>

<p><a href="/places">Back to All Places</a></p>